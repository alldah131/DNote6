<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.dahlstore.dnote6.DetailActivity">


    <EditText
        android:layout_alignParentTop="true"
        android:hint="Title"
        android:inputType="textCapWords"
        android:padding="16dp"
        android:gravity="top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/title_edit_text"/>

    <Button
        android:layout_width="140dp"
        android:layout_height="48dp"
        android:text="Save"
        android:onClick="onSaveClick"
        android:id="@+id/save_button"
        android:backgroundTint="#1976D2"
        android:textColor="#FFFFFF"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/content_edit_text"
        android:layout_alignEnd="@+id/content_edit_text" />

    <EditText
        android:layout_below="@id/title_edit_text"
        android:hint="Content"
        android:inputType="textCapWords"
        android:maxLines="6"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/content_edit_text"/>

    <Button
        android:layout_width="140dp"
        android:layout_height="48dp"
        android:backgroundTint="#1976D2"
        android:textColor="#FFFFFF"
        android:text="Cancel"
        android:onClick="setCancelClick"
        android:layout_alignTop="@+id/save_button"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

</RelativeLayout>
